- set_title

- if admin?
  = link_to t('.new'), new_grave_path, class: 'btn btn-primary right'
  
.page-header
  %h1= t '.title'

- @quarters.each do |quarter|
  %h2= link_to quarter.name, quarter
  - quarter.graves.sort.each do |grave|
    = link_to grave.number, grave
