= simple_form_for @grave do |f|
  = f.association :quarter
  = f.input :number
  = f.input :description
  = f.input :family_name
  = f.input :notes

  .form-actions
    = f.submit nil, class: 'btn btn-primary'
    = cancel_link(@grave.new_record? ? graves_path : grave_path(@grave))
