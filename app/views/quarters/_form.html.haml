= simple_form_for @quarter do |f|
  = f.input :name
  = f.input :short_name
  
  .form-actions
    = f.submit nil, class: 'btn btn-primary'
    = cancel_link(@quarter.new_record? ? quarters_path : quarter_path(@quarter))
